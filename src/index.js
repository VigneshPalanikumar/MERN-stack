import React from "react";
import ReactDOM from "react-dom";
import Car from './input';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Car/>
  </React.StrictMode>
);